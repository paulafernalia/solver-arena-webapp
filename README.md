# Solver Arena Web App

A responsive web dashboard built with [Plotly Dash](https://dash.plotly.com/) to interact with [Solver Arena](https://github.com/pataq21/SolverArena). 

---

## Dependencies

This project uses [**uv**](https://github.com/astral-sh/uv) for dependency and environment management.

The list of dependencies can be found in `pyproject.toml`. Requires Python 3.13+.

---

## Usage

1. **Clone the repository**
    ```bash
    git clone https://github.com/paulafernalia/solver-arena-webapp
    cd solver-arena-webapp
    ```

2. **Install dependencies**
    ```bash
    uv venv
    uv sync
    ``` 

3. **Run in development mode**
    ```bash
    uv run app.py
    ```

---

## License

GPL-3.0 License. See `LICENSE file for details.


--- 

## Project structure

```
.
├── LICENSE
├── README.md                   # Project description
├── Makefile                    # Shortcuts for Unix
│
├── app.py                      # Main entry point for the Dash app
├── layout.py                   # Assembles the full app layout from components
├── callbacks.py                # All Dash callback functions
│
├── pyproject.toml              # Project config (used by uv)
├── uv.lock                     # Lockfile generated by uv
│
├── assets/                     # Static CSS assets automatically used by Dash
│   ├── colors.css
│   ├── fonts.css
│   └── styles.css

├── components/                 # Modular UI components used in layout
│   ├── __init__.py
│   ├── base.py                 # Reusable basic components like titles or text lines
│   ├── buttons.py              # UI buttons (e.g., run button)
│   ├── plots.py                # Plot functions (charts, visualizations)
│   ├── selectors.py            # Selectors (radio buttons, toggles, etc.)
│   └── text.py                 # Text and annotation components

├── utils/                      # Utility modules
│   ├── __init__.py
│   ├── colors.py               # Color constants and helpers
│   ├── data.py                 # Functions to generate or load data for plots
│   ├── fonts.py                # Font constants
│   └── solvers.py              # Solver list
└──
```